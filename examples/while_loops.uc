let test = 0;
while (test != 10) {
    if (test != 5) {
        stdout(8888);
    }
    stdout(test);
    test = test + 1;
}
let var = 10;
stdout(var);
while (var != 100) {
    stdout(var);
    if (var != 20) {
        stdout(4444);
    }
    var = var + 10;
}